import { IRestableService } from '@obel/restable';
import {AppService} from './app.service'


describe('AppService', () => {
    let appService:IRestableService
    beforeEach(() => {
        appService = new AppService();
    });
    it('should do stuff', () => {
        fail("Nothing implemented ye t ")
    });
});